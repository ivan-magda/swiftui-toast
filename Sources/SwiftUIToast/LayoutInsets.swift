import Foundation

/// Defines layout inset values for consistent spacing
///
/// This enum provides standard spacing values used throughout the toast UI.
enum LayoutInsets {
    /// Small inset value for tight spacing
    static let smallInset: CGFloat = 8

    /// Default inset value for standard spacing
    static let defaultInset: CGFloat = 16
}
